{{ attach_library('upss/upss.catalog') }}
<div class="phone">
    <div class="phone-image">
        <a href="#" class="phone-link">{{ image }}</a>
    </div>
    <div class="phone-description">
        <a href="#"><strong>{{ phone['name'] }}</strong></a>
        <p>
            {{ phone['description'] }}
        </p>
    </div>
</div>